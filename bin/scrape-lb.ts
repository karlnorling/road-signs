/**
 * scrape-lb.ts — Lebanon
 * Vienna Convention-adjacent. No SVG Commons category — Commons-only stub.
 * Run via: yarn update --country=lb
 */
import { createViennaScraper } from './scrape-vienna';
// No SVG Commons categories exist for Lebanon.
export default createViennaScraper({
  cc: 'lb',
  country: 'Lebanon',
  commonsCategories: [],
});
