---
title: Poland (PL)
description: Polish road signs — categories, installation, and usage.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';


{/* HERO:START */}

import CountryHero from '../../../components/CountryHero.astro';

<CountryHero cc="pl" />

{/* HERO:END */}

The `@road-signs/pl` package contains Poland road signs from the **Prawo o ruchu drogowym**, using A/B/C/D/E/F/G series.
Signs are sourced from Wikipedia / Wikimedia Commons. Signs are in the public domain.

## Installation

```sh
npm install @road-signs/pl
```

## Sign categories

| Category | Series | Description |
| -------- | ------ | ----------- |
| `warning` | A series | Ostrzegawcze (warning) — triangular, red border |
| `priority` | A series (priority) | Pierwszeństwo (right-of-way) signs |
| `prohibitory` | B series | Zakazu (prohibition) — circular, red border |
| `mandatory` | C series | Nakazu (obligation) — circular, blue background |
| `information` | D / E / F series | Informacyjne (information) and direction signs |

## Usage

<Tabs>
  <TabItem label="TypeScript">
    ```ts
    import { signs, getSign, getSignByCode, getSignsByCategory } from '@road-signs/pl';

    // All signs
    signs.length;

    // By slug ID
    const sign = getSign("a-1-dangerous-bend");

    // By sign code
    const s = getSignByCode("A-1");

    // All prohibitory signs
    const filtered = getSignsByCategory('prohibitory');
    ```
  </TabItem>
  <TabItem label="React">
    ```tsx
    import { getSignsByCategory } from '@road-signs/pl';
    import { RoadSign } from '@road-signs/react';

    const warnings = getSignsByCategory('warning');

    export default function WarningGrid() {
      return (
        <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>
          {warnings.map((sign) => (
            <RoadSign key={sign.id} sign={sign} size={64} />
          ))}
        </div>
      );
    }
    ```
  </TabItem>
  <TabItem label="Vue 3">
    ```vue
    <script setup>
    import { getSignsByCategory } from '@road-signs/pl';
    import { RoadSign } from '@road-signs/vue';

    const warnings = getSignsByCategory('warning');
    </script>

    <template>
      <div style="display:flex;gap:8px;flex-wrap:wrap">
        <RoadSign v-for="sign in warnings" :key="sign.id" :sign="sign" :size="64" />
      </div>
    </template>
    ```
  </TabItem>
</Tabs>


{/* COVERAGE:START */}

## Coverage

| Metric | Value |
| ------ | ----- |
| Signs scraped | 401 |
| Signs with SVG | 401 |
| Missing SVG | 0 |
| Overall | **100%** |

| Category | Scraped | With SVG | Missing | % |
| -------- | ------: | -------: | ------: | -: |
| `warning` | 44 | 44 | 0 | 100% |
| `priority` | 1 | 1 | 0 | 100% |
| `prohibitory` | 53 | 53 | 0 | 100% |
| `mandatory` | 21 | 21 | 0 | 100% |
| `information` | 282 | 282 | 0 | 100% |

**Standard:** Prawo o ruchu drogowym — Vienna Convention

**SVG sources:**

- [Wikimedia Commons — Road signs in Poland](https://commons.wikimedia.org/wiki/Category:SVG_road_signs_in_Poland)

:::note
Several code series (A–W) scraped from Wikipedia lack matching SVG assets on Commons.
:::

_Coverage data is also published in the [`@road-signs/docs`](https://www.npmjs.com/package/@road-signs/docs) package._

{/* COVERAGE:END */}

## TypeScript types

```ts
import type { PLSign, PLCategory } from '@road-signs/pl';

type PLCategory =
  | 'information'
  | 'mandatory'
  | 'priority'
  | 'prohibitory'
  | 'warning';

interface PLSign {
  id: string;          // e.g. "a-1-dangerous-bend"
  code: string;        // Series code, e.g. "A-1"
  name: string;
  description: string;
  category: PLCategory;
  svg: string;         // inline SVG string
  assets: {
    svg: string;
    png: Record<number, string>;
    jpg: Record<number, string>;
    webp: Record<number, string>;
  };
}
```
