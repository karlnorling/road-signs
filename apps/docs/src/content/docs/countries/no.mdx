---
title: Norway (NO)
description: Norwegian Skiltforskriften road signs — categories, installation, and usage.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';


{/* HERO:START */}

import CountryHero from '../../../components/CountryHero.astro';

<CountryHero cc="no" />

{/* HERO:END */}

{/* QUICK_FACTS:START */}

## Quick facts

| Field | Value |
| ----- | ----- |
| Drive side | Right-hand |
| Language(s) | Norwegian |
| Regulator | Statens vegvesen |
| Code format | `100 / 202 / 306` |
| Standard | Skiltforskriften — Vienna Convention |

{/* QUICK_FACTS:END */}

The `@road-signs/no` package contains Norway road signs from the **Skiltforskriften**, Norway's road sign regulations which follow the Vienna Convention.
Signs are sourced from Wikipedia / Wikimedia Commons. Signs are in the public domain.

## Installation

```sh
npm install @road-signs/no
```

## Sign categories

| Category | Series | Description |
| -------- | ------ | ----------- |
| `warning` | 1xx | Advarselsskilt — hazard warnings |
| `priority` | 2xx | Vikepliktsskilt — right-of-way |
| `prohibitory` | 3xx | Forbudsskilt — prohibitions |
| `mandatory` | 4xx | Påbudsskilt — compulsory actions |
| `information` | 5–7xx | Opplysningsskilt — information signs |

{/* USAGE:START */}

## Usage

<Tabs>
  <TabItem label="TypeScript">
    ```ts
    import { signs, getSign, getSignByCode, getSignsByCategory } from '@road-signs/no';

    // Example below uses the real NO sign "367" — adjust to taste.

    // All signs
    signs.length;

    // By slug ID
    const sign = getSign("367-speed-limit-zone-for-small-electric-vehicles");

    // By sign code
    const s = getSignByCode("367");

    // All prohibitory signs
    const filtered = getSignsByCategory("prohibitory");
    ```
  </TabItem>
  <TabItem label="React">
    ```tsx
    import { getSignsByCategory } from '@road-signs/no';
    import { RoadSign } from '@road-signs/react';

    const prohibitorySigns = getSignsByCategory("prohibitory");

    export default function NOGrid() {
      return (
        <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>
          {prohibitorySigns.map((sign) => (
            <RoadSign key={sign.id} sign={sign} size={64} />
          ))}
        </div>
      );
    }
    ```
  </TabItem>
  <TabItem label="Vue 3">
    ```vue
    <script setup>
    import { getSignsByCategory } from '@road-signs/no';
    import { RoadSign } from '@road-signs/vue';

    const prohibitorySigns = getSignsByCategory("prohibitory");
    </script>

    <template>
      <div style="display:flex;gap:8px;flex-wrap:wrap">
        <RoadSign v-for="sign in prohibitorySigns" :key="sign.id" :sign="sign" :size="64" />
      </div>
    </template>
    ```
  </TabItem>
</Tabs>

{/* USAGE:END */}

{/* COVERAGE:START */}

## Coverage

| Metric | Value |
| ------ | ----- |
| Signs scraped | 270 |
| Signs with SVG | 270 |
| Missing SVG | 0 |
| Overall | **100%** |

| Category | Scraped | With SVG | Missing | % |
| -------- | ------: | -------: | ------: | -: |
| `warning` | 47 | 47 | 0 | 100% |
| `priority` | 7 | 7 | 0 | 100% |
| `prohibitory` | 46 | 46 | 0 | 100% |
| `mandatory` | 12 | 12 | 0 | 100% |
| `information` | 158 | 158 | 0 | 100% |

**Standard:** Skiltforskriften — Vienna Convention

**SVG sources:**

- [Wikimedia Commons — Road signs in Norway](https://commons.wikimedia.org/wiki/Category:SVG_road_signs_in_Norway)

:::note
Codes are numeric: 1xx (warning), 2xx (priority), 3xx (prohibitory), 4xx (mandatory), 5–7xx (information).
:::

_Coverage data is also published in the [`@road-signs/docs`](https://www.npmjs.com/package/@road-signs/docs) package._

{/* COVERAGE:END */}

## TypeScript types

```ts
import type { NOSign, NOCategory } from '@road-signs/no';

type NOCategory =
  | 'information'
  | 'mandatory'
  | 'priority'
  | 'prohibitory'
  | 'warning';

interface NOSign {
  id: string;          // e.g. "104-dangerous-bend"
  code: string;        // e.g. "104"
  name: string;
  description: string;
  category: NOCategory;
  svg: string;         // inline SVG string
  assets: {
    svg: string;
    png: Record<number, string>;
    jpg: Record<number, string>;
    webp: Record<number, string>;
  };
}
```
