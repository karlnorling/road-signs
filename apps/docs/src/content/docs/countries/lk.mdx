---
title: Sri Lanka (LK)
description: Sri Lanka road signs — categories, installation, and usage.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';

The `@road-signs/lk` package contains Sri Lanka road signs from the **Vienna Convention (Motor Traffic Act)**.
Left-hand traffic. Signs are sourced from Wikimedia Commons and are in the public domain.

## Installation

```sh
npm install @road-signs/lk
```

## Sign categories

| Category | Series | Description |
| -------- | ------ | ----------- |
| `warning` | Warning | Hazard and warning signs |
| `priority` | Priority | Priority and right-of-way signs |
| `prohibitory` | Regulatory | Regulatory and prohibition signs |

## Usage

<Tabs>
  <TabItem label="TypeScript">
    ```ts
    import { signs, getSign, getSignByCode, getSignsByCategory } from '@road-signs/lk';

    // All signs
    signs.length;

    // By slug ID
    const sign = getSign("example-sign");

    // By sign code
    const s = getSignByCode("W1");

    // All warning signs
    const filtered = getSignsByCategory('warning');
    ```
  </TabItem>
  <TabItem label="React">
    ```tsx
    import { getSignsByCategory } from '@road-signs/lk';
    import { RoadSign } from '@road-signs/react';

    const warnings = getSignsByCategory('warning');

    export default function WarningGrid() {
      return (
        <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>
          {warnings.map((sign) => (
            <RoadSign key={sign.id} sign={sign} size={64} />
          ))}
        </div>
      );
    }
    ```
  </TabItem>
  <TabItem label="Vue 3">
    ```vue
    <script setup>
    import { getSignsByCategory } from '@road-signs/lk';
    import { RoadSign } from '@road-signs/vue';

    const warnings = getSignsByCategory('warning');
    </script>

    <template>
      <div style="display:flex;gap:8px;flex-wrap:wrap">
        <RoadSign v-for="sign in warnings" :key="sign.id" :sign="sign" :size="64" />
      </div>
    </template>
    ```
  </TabItem>
</Tabs>


{/* COVERAGE:START */}

## Coverage

| Metric | Value |
| ------ | ----- |
| Signs scraped | 184 |
| Signs with SVG | 184 |
| Missing SVG | 0 |
| Overall | **100%** |

| Category | Scraped | With SVG | Missing | % |
| -------- | ------: | -------: | ------: | -: |
| `warning` | 49 | 49 | 0 | 100% |
| `priority` | 4 | 4 | 0 | 100% |
| `prohibitory` | 26 | 26 | 0 | 100% |
| `mandatory` | 53 | 53 | 0 | 100% |
| `information` | 52 | 52 | 0 | 100% |

**Standard:** British-derived (Motor Traffic Act)

**SVG sources:**

- [Wikipedia — Road signs in Sri Lanka](https://en.wikipedia.org/wiki/Road_signs_in_Sri_Lanka)
- [Wikimedia Commons — SVG road signs in Sri Lanka](https://commons.wikimedia.org/wiki/Category:SVG_road_signs_in_Sri_Lanka)

_Coverage data is also published in the [`@road-signs/docs`](https://www.npmjs.com/package/@road-signs/docs) package._

{/* COVERAGE:END */}

## TypeScript types

```ts
import type { LKSign, LKCategory } from '@road-signs/lk';
```
