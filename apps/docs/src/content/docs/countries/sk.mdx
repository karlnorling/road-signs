---
title: Slovakia (SK)
description: Slovak road signs — categories, installation, and usage.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';


{/* HERO:START */}

import CountryHero from '../../../components/CountryHero.astro';

<CountryHero cc="sk" />

{/* HERO:END */}

{/* QUICK_FACTS:START */}

## Quick facts

| Field | Value |
| ----- | ----- |
| Drive side | Right-hand |
| Language(s) | Slovak |
| Regulator | MDV SR |
| Code format | `A-1 / B-1 / C-1` |
| Standard | Zákon č. 8/2009 Z. z. — Vienna Convention |

{/* QUICK_FACTS:END */}

The `@road-signs/sk` package contains Slovak road signs under **zákon č. 8/2009 Z. z.**
Signs are sourced from Wikimedia Commons and are in the public domain.

## Installation

```sh
npm install @road-signs/sk
```

## Sign categories

| Category | Series | Description |
| -------- | ------ | ----------- |
| `warning` | A series | Výstražné značky — hazard warnings |
| `priority` | B series | Značky upravujúce prednosť — right-of-way |
| `prohibitory` | B/C series | Zákazové a príkazové značky — prohibitions |
| `mandatory` | C series | Príkazové značky — mandatory |
| `information` | IP/IJ/IS series | Informatívne značky — information |

{/* USAGE:START */}

## Usage

<Tabs>
  <TabItem label="TypeScript">
    ```ts
    import { signs, getSign, getSignByCode, getSignsByCategory } from '@road-signs/sk';

    // Example below uses the real SK sign "1-41-school-zone" — adjust to taste.

    // All signs
    signs.length;

    // By slug ID
    const sign = getSign("1-41-school-zone-1-41-school-zone");

    // By sign code
    const s = getSignByCode("1-41-school-zone");

    // All warning signs
    const filtered = getSignsByCategory("warning");
    ```
  </TabItem>
  <TabItem label="React">
    ```tsx
    import { getSignsByCategory } from '@road-signs/sk';
    import { RoadSign } from '@road-signs/react';

    const warningSigns = getSignsByCategory("warning");

    export default function SKGrid() {
      return (
        <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>
          {warningSigns.map((sign) => (
            <RoadSign key={sign.id} sign={sign} size={64} />
          ))}
        </div>
      );
    }
    ```
  </TabItem>
  <TabItem label="Vue 3">
    ```vue
    <script setup>
    import { getSignsByCategory } from '@road-signs/sk';
    import { RoadSign } from '@road-signs/vue';

    const warningSigns = getSignsByCategory("warning");
    </script>

    <template>
      <div style="display:flex;gap:8px;flex-wrap:wrap">
        <RoadSign v-for="sign in warningSigns" :key="sign.id" :sign="sign" :size="64" />
      </div>
    </template>
    ```
  </TabItem>
</Tabs>

{/* USAGE:END */}

{/* COVERAGE:START */}

## Coverage

| Metric | Value |
| ------ | ----- |
| Signs scraped | 133 |
| Signs with SVG | 133 |
| Missing SVG | 0 |
| Overall | **100%** |

| Category | Scraped | With SVG | Missing | % |
| -------- | ------: | -------: | ------: | -: |
| `warning` | 40 | 40 | 0 | 100% |
| `priority` | 6 | 6 | 0 | 100% |
| `prohibitory` | 39 | 39 | 0 | 100% |
| `mandatory` | 20 | 20 | 0 | 100% |
| `information` | 28 | 28 | 0 | 100% |

**Standard:** Zákon č. 8/2009 Z. z. — Vienna Convention

**SVG sources:**

- [Wikimedia Commons — Road signs in Slovakia](https://commons.wikimedia.org/wiki/Category:SVG_road_signs_in_Slovakia)

_Coverage data is also published in the [`@road-signs/docs`](https://www.npmjs.com/package/@road-signs/docs) package._

{/* COVERAGE:END */}

## TypeScript types

```ts
import type { SKSign, SKCategory } from '@road-signs/sk';

type SKCategory =
  | 'information'
  | 'mandatory'
  | 'priority'
  | 'prohibitory'
  | 'warning';

interface SKSign {
  id: string;          // e.g. "a1-sharp-bend"
  code: string;        // sign code, e.g. "A1"
  name: string;
  description: string;
  category: SKCategory;
  svg: string;         // inline SVG string
  assets: {
    svg: string;
    png: Record<number, string>;
    jpg: Record<number, string>;
    webp: Record<number, string>;
  };
}
```
