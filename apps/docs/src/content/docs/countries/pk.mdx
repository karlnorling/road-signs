---
title: Pakistan (PK)
description: Pakistan road signs — categories, installation, and usage.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';


{/* HERO:START */}

import CountryHero from '../../../components/CountryHero.astro';

<CountryHero cc="pk" />

{/* HERO:END */}

{/* QUICK_FACTS:START */}

## Quick facts

| Field | Value |
| ----- | ----- |
| Drive side | Left-hand |
| Language(s) | Urdu, English |
| Regulator | NHA |
| Standard | British-derived (National Highway Authority) |

{/* QUICK_FACTS:END */}

The `@road-signs/pk` package contains Pakistan road signs from the **Vienna Convention-aligned** standard.
Left-hand traffic. Signs are sourced from Wikimedia Commons and are in the public domain.

## Installation

```sh
npm install @road-signs/pk
```

## Sign categories

| Category | Series | Description |
| -------- | ------ | ----------- |
| `warning` | Warning | Hazard and warning signs |
| `priority` | Priority | Priority and right-of-way signs |
| `prohibitory` | Regulatory | Regulatory and prohibition signs |
| `mandatory` | Mandatory | Mandatory instruction signs |

{/* USAGE:START */}

## Usage

<Tabs>
  <TabItem label="TypeScript">
    ```ts
    import { signs, getSign, getSignByCode, getSignsByCategory } from '@road-signs/pk';

    // Example below uses the real PK sign "W-01" — adjust to taste.

    // All signs
    signs.length;

    // By slug ID
    const sign = getSign("w-01-pakistan-road-sign");

    // By sign code
    const s = getSignByCode("W-01");

    // All information signs
    const filtered = getSignsByCategory("information");
    ```
  </TabItem>
  <TabItem label="React">
    ```tsx
    import { getSignsByCategory } from '@road-signs/pk';
    import { RoadSign } from '@road-signs/react';

    const informationSigns = getSignsByCategory("information");

    export default function PKGrid() {
      return (
        <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>
          {informationSigns.map((sign) => (
            <RoadSign key={sign.id} sign={sign} size={64} />
          ))}
        </div>
      );
    }
    ```
  </TabItem>
  <TabItem label="Vue 3">
    ```vue
    <script setup>
    import { getSignsByCategory } from '@road-signs/pk';
    import { RoadSign } from '@road-signs/vue';

    const informationSigns = getSignsByCategory("information");
    </script>

    <template>
      <div style="display:flex;gap:8px;flex-wrap:wrap">
        <RoadSign v-for="sign in informationSigns" :key="sign.id" :sign="sign" :size="64" />
      </div>
    </template>
    ```
  </TabItem>
</Tabs>

{/* USAGE:END */}

{/* COVERAGE:START */}

## Coverage

| Metric | Value |
| ------ | ----- |
| Signs scraped | 166 |
| Signs with SVG | 166 |
| Missing SVG | 0 |
| Overall | **100%** |

| Category | Scraped | With SVG | Missing | % |
| -------- | ------: | -------: | ------: | -: |
| `warning` | 43 | 43 | 0 | 100% |
| `priority` | 0 | 0 | 0 | — |
| `prohibitory` | 38 | 38 | 0 | 100% |
| `mandatory` | 0 | 0 | 0 | — |
| `information` | 85 | 85 | 0 | 100% |

**Standard:** British-derived (National Highway Authority)

**SVG sources:**

- [Wikipedia — Road signs in Pakistan](https://en.wikipedia.org/wiki/Road_signs_in_Pakistan)
- [Wikimedia Commons — SVG road signs in Pakistan](https://commons.wikimedia.org/wiki/Category:SVG_road_signs_in_Pakistan)

_Coverage data is also published in the [`@road-signs/docs`](https://www.npmjs.com/package/@road-signs/docs) package._

{/* COVERAGE:END */}

## TypeScript types

```ts
import type { PKSign, PKCategory } from '@road-signs/pk';
```
