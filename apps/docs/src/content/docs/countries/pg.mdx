---
title: Papua New Guinea (PG)
description: Papua New Guinea road signs — categories, installation, and usage.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';

The `@road-signs/pg` package contains Papua New Guinea road signs (British-derived (Motor Traffic Act)).
Signs are sourced from Wikimedia Commons and are in the public domain.

## Installation

```sh
npm install @road-signs/pg
```

## Sign categories

| Category | Series | Description |
| -------- | ------ | ----------- |
| `warning` | Warning | Hazard and warning signs |
| `prohibitory` | Prohibitory | Prohibition and restriction signs |
| `mandatory` | Mandatory | Mandatory instruction signs |
| `information` | Information / Service / Additional | Information, service, and additional plate signs |

{/* USAGE:START */}

## Usage

<Tabs>
  <TabItem label="TypeScript">
    ```ts
    import { signs, getSign, getSignByCode, getSignsByCategory } from '@road-signs/pg';

    // Placeholders — replace with real codes once @road-signs/pg ships a registry.

    // All signs
    signs.length;

    // By slug ID
    const sign = getSign("example-sign");

    // By sign code
    const s = getSignByCode("EXAMPLE-CODE");

    // All warning signs
    const filtered = getSignsByCategory("warning");
    ```
  </TabItem>
  <TabItem label="React">
    ```tsx
    import { getSignsByCategory } from '@road-signs/pg';
    import { RoadSign } from '@road-signs/react';

    const warningSigns = getSignsByCategory("warning");

    export default function PGGrid() {
      return (
        <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>
          {warningSigns.map((sign) => (
            <RoadSign key={sign.id} sign={sign} size={64} />
          ))}
        </div>
      );
    }
    ```
  </TabItem>
  <TabItem label="Vue 3">
    ```vue
    <script setup>
    import { getSignsByCategory } from '@road-signs/pg';
    import { RoadSign } from '@road-signs/vue';

    const warningSigns = getSignsByCategory("warning");
    </script>

    <template>
      <div style="display:flex;gap:8px;flex-wrap:wrap">
        <RoadSign v-for="sign in warningSigns" :key="sign.id" :sign="sign" :size="64" />
      </div>
    </template>
    ```
  </TabItem>
</Tabs>

{/* USAGE:END */}

{/* COVERAGE:START */}

## Coverage

| Metric | Value |
| ------ | ----- |
| Signs scraped | 0 |
| Signs with SVG | 0 |
| Missing SVG | 0 |
| Overall | **—** |

| Category | Scraped | With SVG | Missing | % |
| -------- | ------: | -------: | ------: | -: |
| `warning` | 0 | 0 | 0 | — |
| `priority` | 0 | 0 | 0 | — |
| `prohibitory` | 0 | 0 | 0 | — |
| `mandatory` | 0 | 0 | 0 | — |
| `information` | 0 | 0 | 0 | — |

**Standard:** British-derived (Motor Traffic Act / Road User Rules 2017)

**SVG sources:**

- [Wikimedia Commons — SVG road signs in Papua New Guinea](https://commons.wikimedia.org/wiki/Category:SVG_road_signs_in_Papua_New_Guinea)
- [Road Traffic Authority Papua New Guinea](https://www.rta.gov.pg/)
- [Road User Rules 2017 (PDF)](https://rta.gov.pg/pdfs/licences&approvals/RTR_RoadUserRules2017.pdf)

:::note
No SVG assets currently on Wikimedia Commons. Papua New Guinea uses a British-derived sign system governed by the Road Traffic Authority (RTA) under the Road User Rules 2017.
:::

_Coverage data is also published in the [`@road-signs/docs`](https://www.npmjs.com/package/@road-signs/docs) package._

{/* COVERAGE:END */}

## TypeScript types

```ts
import type { PGSign, PGCategory } from '@road-signs/pg';
```
