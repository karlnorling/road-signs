---
title: El Salvador (SV)
description: El Salvador road signs — categories, installation, and usage.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';


{/* HERO:START */}

import CountryHero from '../../../components/CountryHero.astro';

<CountryHero cc="sv" />

{/* HERO:END */}

The `@road-signs/sv` package contains El Salvador road signs (SIECA).
Signs are sourced from Wikimedia Commons and are in the public domain.

## Installation

```sh
npm install @road-signs/sv
```

## Sign categories

| Category | Series | Description |
| -------- | ------ | ----------- |
| `warning` | Warning | Hazard and warning signs |
| `prohibitory` | Prohibitory | Prohibition and restriction signs |
| `information` | Information / Service / Additional | Information, service, and additional plate signs |

{/* USAGE:START */}

## Usage

<Tabs>
  <TabItem label="TypeScript">
    ```ts
    import { signs, getSign, getSignByCode, getSignsByCategory } from '@road-signs/sv';

    // Example below uses the real SV sign "P-1-1" — adjust to taste.

    // All signs
    signs.length;

    // By slug ID
    const sign = getSign("p-1-1-sharp-curve-to-the-right-left-if-reversed");

    // By sign code
    const s = getSignByCode("P-1-1");

    // All warning signs
    const filtered = getSignsByCategory("warning");
    ```
  </TabItem>
  <TabItem label="React">
    ```tsx
    import { getSignsByCategory } from '@road-signs/sv';
    import { RoadSign } from '@road-signs/react';

    const warningSigns = getSignsByCategory("warning");

    export default function SVGrid() {
      return (
        <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>
          {warningSigns.map((sign) => (
            <RoadSign key={sign.id} sign={sign} size={64} />
          ))}
        </div>
      );
    }
    ```
  </TabItem>
  <TabItem label="Vue 3">
    ```vue
    <script setup>
    import { getSignsByCategory } from '@road-signs/sv';
    import { RoadSign } from '@road-signs/vue';

    const warningSigns = getSignsByCategory("warning");
    </script>

    <template>
      <div style="display:flex;gap:8px;flex-wrap:wrap">
        <RoadSign v-for="sign in warningSigns" :key="sign.id" :sign="sign" :size="64" />
      </div>
    </template>
    ```
  </TabItem>
</Tabs>

{/* USAGE:END */}

{/* COVERAGE:START */}

## Coverage

| Metric | Value |
| ------ | ----- |
| Signs scraped | 420 |
| Signs with SVG | 420 |
| Missing SVG | 0 |
| Overall | **100%** |

| Category | Scraped | With SVG | Missing | % |
| -------- | ------: | -------: | ------: | -: |
| `warning` | 125 | 125 | 0 | 100% |
| `priority` | 0 | 0 | 0 | — |
| `prohibitory` | 192 | 192 | 0 | 100% |
| `mandatory` | 0 | 0 | 0 | — |
| `information` | 103 | 103 | 0 | 100% |

**Standard:** Manual Centroamericano de Dispositivos Uniformes (SIECA)

**SVG sources:**

- [Wikipedia — Road signs in El Salvador](https://en.wikipedia.org/wiki/Road_signs_in_El_Salvador)
- [Wikimedia Commons — SVG road signs in El Salvador](https://commons.wikimedia.org/wiki/Category:SVG_road_signs_in_El_Salvador)

_Coverage data is also published in the [`@road-signs/docs`](https://www.npmjs.com/package/@road-signs/docs) package._

{/* COVERAGE:END */}

## TypeScript types

```ts
import type { SVSign, SVCategory } from '@road-signs/sv';
```
