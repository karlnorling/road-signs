---
title: Burkina Faso (BF)
description: Burkina Faso road signs — categories, installation, and usage.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';

The `@road-signs/bf` package contains Burkina Faso road signs.

## Installation

```sh
npm install @road-signs/bf
```

{/* USAGE:START */}

## Usage

<Tabs>
  <TabItem label="TypeScript">
    ```ts
    import { signs, getSign, getSignByCode, getSignsByCategory } from '@road-signs/bf';

    // Placeholders — replace with real codes once @road-signs/bf ships a registry.

    // All signs
    signs.length;

    // By slug ID
    const sign = getSign("example-sign");

    // By sign code
    const s = getSignByCode("EXAMPLE-CODE");

    // All warning signs
    const filtered = getSignsByCategory("warning");
    ```
  </TabItem>
  <TabItem label="React">
    ```tsx
    import { getSignsByCategory } from '@road-signs/bf';
    import { RoadSign } from '@road-signs/react';

    const warningSigns = getSignsByCategory("warning");

    export default function BFGrid() {
      return (
        <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>
          {warningSigns.map((sign) => (
            <RoadSign key={sign.id} sign={sign} size={64} />
          ))}
        </div>
      );
    }
    ```
  </TabItem>
  <TabItem label="Vue 3">
    ```vue
    <script setup>
    import { getSignsByCategory } from '@road-signs/bf';
    import { RoadSign } from '@road-signs/vue';

    const warningSigns = getSignsByCategory("warning");
    </script>

    <template>
      <div style="display:flex;gap:8px;flex-wrap:wrap">
        <RoadSign v-for="sign in warningSigns" :key="sign.id" :sign="sign" :size="64" />
      </div>
    </template>
    ```
  </TabItem>
</Tabs>

{/* USAGE:END */}

{/* COVERAGE:START */}

## Coverage

| Metric | Value |
| ------ | ----- |
| Signs scraped | 0 |
| Signs with SVG | 0 |
| Missing SVG | 0 |
| Overall | **—** |

| Category | Scraped | With SVG | Missing | % |
| -------- | ------: | -------: | ------: | -: |

**Standard:** Code de la route (French-influenced)

**SVG sources:**

- [Wikipedia — Transport in Burkina Faso](https://en.wikipedia.org/wiki/Transport_in_Burkina_Faso)

:::note
Empty package: no publicly available SVG sign catalogue located. Likely uses the French Vienna-Convention sign system with local variants.
:::

_Coverage data is also published in the [`@road-signs/docs`](https://www.npmjs.com/package/@road-signs/docs) package._

{/* COVERAGE:END */}

## TypeScript types

```ts
import type { BFSign, BFCategory } from '@road-signs/bf';
```
