---
title: Moldova (MD)
description: Moldova road signs — categories, installation, and usage.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';


{/* HERO:START */}

import CountryHero from '../../../components/CountryHero.astro';

<CountryHero cc="md" />

{/* HERO:END */}

The `@road-signs/md` package contains Moldova road signs from the **Vienna Convention**.
Signs are sourced from Wikimedia Commons and are in the public domain.

## Installation

```sh
npm install @road-signs/md
```

## Sign categories

| Category | Series | Description |
| -------- | ------ | ----------- |
| `warning` | Warning | Hazard and warning signs |
| `priority` | Priority | Priority and right-of-way signs |
| `prohibitory` | Prohibitory | Prohibition and restriction signs |
| `mandatory` | Mandatory | Mandatory instruction signs |
| `information` | Information / Service / Tourist / Additional | Information, service, tourist, and additional plate signs |

{/* USAGE:START */}

## Usage

<Tabs>
  <TabItem label="TypeScript">
    ```ts
    import { signs, getSign, getSignByCode, getSignsByCategory } from '@road-signs/md';

    // Example below uses the real MD sign "M3" — adjust to taste.

    // All signs
    signs.length;

    // By slug ID
    const sign = getSign("m3-moldova-roadsign");

    // By sign code
    const s = getSignByCode("M3");

    // All information signs
    const filtered = getSignsByCategory("information");
    ```
  </TabItem>
  <TabItem label="React">
    ```tsx
    import { getSignsByCategory } from '@road-signs/md';
    import { RoadSign } from '@road-signs/react';

    const informationSigns = getSignsByCategory("information");

    export default function MDGrid() {
      return (
        <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>
          {informationSigns.map((sign) => (
            <RoadSign key={sign.id} sign={sign} size={64} />
          ))}
        </div>
      );
    }
    ```
  </TabItem>
  <TabItem label="Vue 3">
    ```vue
    <script setup>
    import { getSignsByCategory } from '@road-signs/md';
    import { RoadSign } from '@road-signs/vue';

    const informationSigns = getSignsByCategory("information");
    </script>

    <template>
      <div style="display:flex;gap:8px;flex-wrap:wrap">
        <RoadSign v-for="sign in informationSigns" :key="sign.id" :sign="sign" :size="64" />
      </div>
    </template>
    ```
  </TabItem>
</Tabs>

{/* USAGE:END */}

{/* COVERAGE:START */}

## Coverage

| Metric | Value |
| ------ | ----- |
| Signs scraped | 296 |
| Signs with SVG | 294 |
| Missing SVG | 2 |
| Overall | **99%** |

| Category | Scraped | With SVG | Missing | % |
| -------- | ------: | -------: | ------: | -: |
| `warning` | 51 | 49 | 2 | 96% |
| `priority` | 7 | 7 | 0 | 100% |
| `prohibitory` | 39 | 39 | 0 | 100% |
| `mandatory` | 25 | 25 | 0 | 100% |
| `information` | 174 | 174 | 0 | 100% |

**Standard:** Vienna Convention

**SVG sources:**

- [Wikipedia — Road signs in Moldova](https://en.wikipedia.org/wiki/Road_signs_in_Moldova)
- [Wikimedia Commons — SVG road signs in Moldova](https://commons.wikimedia.org/wiki/Category:SVG_road_signs_in_Moldova)

_Coverage data is also published in the [`@road-signs/docs`](https://www.npmjs.com/package/@road-signs/docs) package._

{/* COVERAGE:END */}

## TypeScript types

```ts
import type { MDSign, MDCategory } from '@road-signs/md';
```
