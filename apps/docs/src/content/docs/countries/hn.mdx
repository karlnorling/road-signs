---
title: Honduras (HN)
description: Honduras road signs — categories, installation, and usage.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';


{/* HERO:START */}

import CountryHero from '../../../components/CountryHero.astro';

<CountryHero cc="hn" />

{/* HERO:END */}

The `@road-signs/hn` package contains Honduras road signs from the **MUTCD-based (SICA/Central American standard)**.
Signs are sourced from Wikimedia Commons and are in the public domain.

## Installation

```sh
npm install @road-signs/hn
```

## Sign categories

| Category | Series | Description |
| -------- | ------ | ----------- |
| `warning` | Warning | Hazard and warning signs |
| `prohibitory` | Regulatory | Regulatory and prohibition signs |

{/* USAGE:START */}

## Usage

<Tabs>
  <TabItem label="TypeScript">
    ```ts
    import { signs, getSign, getSignByCode, getSignsByCategory } from '@road-signs/hn';

    // Example below uses the real HN sign "P-1-1" — adjust to taste.

    // All signs
    signs.length;

    // By slug ID
    const sign = getSign("p-1-1-sharp-curve-to-the-right-left-if-reversed");

    // By sign code
    const s = getSignByCode("P-1-1");

    // All warning signs
    const filtered = getSignsByCategory("warning");
    ```
  </TabItem>
  <TabItem label="React">
    ```tsx
    import { getSignsByCategory } from '@road-signs/hn';
    import { RoadSign } from '@road-signs/react';

    const warningSigns = getSignsByCategory("warning");

    export default function HNGrid() {
      return (
        <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>
          {warningSigns.map((sign) => (
            <RoadSign key={sign.id} sign={sign} size={64} />
          ))}
        </div>
      );
    }
    ```
  </TabItem>
  <TabItem label="Vue 3">
    ```vue
    <script setup>
    import { getSignsByCategory } from '@road-signs/hn';
    import { RoadSign } from '@road-signs/vue';

    const warningSigns = getSignsByCategory("warning");
    </script>

    <template>
      <div style="display:flex;gap:8px;flex-wrap:wrap">
        <RoadSign v-for="sign in warningSigns" :key="sign.id" :sign="sign" :size="64" />
      </div>
    </template>
    ```
  </TabItem>
</Tabs>

{/* USAGE:END */}

{/* COVERAGE:START */}

## Coverage

| Metric | Value |
| ------ | ----- |
| Signs scraped | 446 |
| Signs with SVG | 446 |
| Missing SVG | 0 |
| Overall | **100%** |

| Category | Scraped | With SVG | Missing | % |
| -------- | ------: | -------: | ------: | -: |
| `warning` | 151 | 151 | 0 | 100% |
| `priority` | 0 | 0 | 0 | — |
| `prohibitory` | 192 | 192 | 0 | 100% |
| `mandatory` | 0 | 0 | 0 | — |
| `information` | 103 | 103 | 0 | 100% |

**Standard:** Manual Centroamericano de Dispositivos Uniformes (SIECA)

**SVG sources:**

- [Wikipedia — Road signs in Central America](https://en.wikipedia.org/wiki/Road_signs_in_Central_America)
- [Wikimedia Commons — SVG road signs in Honduras](https://commons.wikimedia.org/wiki/Category:SVG_road_signs_in_Honduras)

_Coverage data is also published in the [`@road-signs/docs`](https://www.npmjs.com/package/@road-signs/docs) package._

{/* COVERAGE:END */}

## TypeScript types

```ts
import type { HNSign, HNCategory } from '@road-signs/hn';
```
