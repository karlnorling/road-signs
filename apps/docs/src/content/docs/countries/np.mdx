---
title: Nepal (NP)
description: Nepal road signs — categories, installation, and usage.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';


{/* HERO:START */}

import CountryHero from '../../../components/CountryHero.astro';

<CountryHero cc="np" />

{/* HERO:END */}

{/* QUICK_FACTS:START */}

## Quick facts

| Field | Value |
| ----- | ----- |
| Drive side | Left-hand |
| Language(s) | Nepali |
| Regulator | DoTM |
| Standard | British-derived (Department of Roads) |

{/* QUICK_FACTS:END */}

The `@road-signs/np` package contains Nepal road signs (British-derived, left-hand traffic).
Signs are sourced from Wikimedia Commons and are in the public domain.

## Installation

```sh
npm install @road-signs/np
```

## Sign categories

| Category | Series | Description |
| -------- | ------ | ----------- |
| `warning` | Warning | Hazard and warning signs |
| `prohibitory` | Prohibitory | Prohibition and restriction signs |
| `mandatory` | Mandatory | Mandatory instruction signs |
| `information` | Information / Service / Additional | Information, service, and additional plate signs |

{/* USAGE:START */}

## Usage

<Tabs>
  <TabItem label="TypeScript">
    ```ts
    import { signs, getSign, getSignByCode, getSignsByCategory } from '@road-signs/np';

    // Example below uses the real NP sign "B10" — adjust to taste.

    // All signs
    signs.length;

    // By slug ID
    const sign = getSign("b10-sharp-curve-to-the-left-ahead");

    // By sign code
    const s = getSignByCode("B10");

    // All warning signs
    const filtered = getSignsByCategory("warning");
    ```
  </TabItem>
  <TabItem label="React">
    ```tsx
    import { getSignsByCategory } from '@road-signs/np';
    import { RoadSign } from '@road-signs/react';

    const warningSigns = getSignsByCategory("warning");

    export default function NPGrid() {
      return (
        <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>
          {warningSigns.map((sign) => (
            <RoadSign key={sign.id} sign={sign} size={64} />
          ))}
        </div>
      );
    }
    ```
  </TabItem>
  <TabItem label="Vue 3">
    ```vue
    <script setup>
    import { getSignsByCategory } from '@road-signs/np';
    import { RoadSign } from '@road-signs/vue';

    const warningSigns = getSignsByCategory("warning");
    </script>

    <template>
      <div style="display:flex;gap:8px;flex-wrap:wrap">
        <RoadSign v-for="sign in warningSigns" :key="sign.id" :sign="sign" :size="64" />
      </div>
    </template>
    ```
  </TabItem>
</Tabs>

{/* USAGE:END */}

{/* COVERAGE:START */}

## Coverage

| Metric | Value |
| ------ | ----- |
| Signs scraped | 118 |
| Signs with SVG | 118 |
| Missing SVG | 0 |
| Overall | **100%** |

| Category | Scraped | With SVG | Missing | % |
| -------- | ------: | -------: | ------: | -: |
| `warning` | 49 | 49 | 0 | 100% |
| `priority` | 0 | 0 | 0 | — |
| `prohibitory` | 33 | 33 | 0 | 100% |
| `mandatory` | 0 | 0 | 0 | — |
| `information` | 36 | 36 | 0 | 100% |

**Standard:** British-derived (Department of Roads)

**SVG sources:**

- [Wikipedia — Road signs in Nepal](https://en.wikipedia.org/wiki/Road_signs_in_Nepal)
- [Wikimedia Commons — SVG road signs in Nepal](https://commons.wikimedia.org/wiki/Category:SVG_road_signs_in_Nepal)

_Coverage data is also published in the [`@road-signs/docs`](https://www.npmjs.com/package/@road-signs/docs) package._

{/* COVERAGE:END */}

## TypeScript types

```ts
import type { NPSign, NPCategory } from '@road-signs/np';
```
