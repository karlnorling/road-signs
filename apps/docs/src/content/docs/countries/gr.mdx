---
title: Greece (GR)
description: Greek ΚΟΚ road signs — categories, installation, and usage.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';


{/* HERO:START */}

import CountryHero from '../../../components/CountryHero.astro';

<CountryHero cc="gr" />

{/* HERO:END */}

{/* QUICK_FACTS:START */}

## Quick facts

| Field | Value |
| ----- | ----- |
| Drive side | Right-hand |
| Language(s) | Greek |
| Regulator | Greek Ministry of Infrastructure |
| Code format | `K-1 / R-1 / P-1` |
| Standard | ΚΟΚ (Κώδικας Οδικής Κυκλοφορίας) — Vienna Convention |

{/* QUICK_FACTS:END */}

The `@road-signs/gr` package contains Greek road signs from the **ΚΟΚ (Κώδικας Οδικής Κυκλοφορίας)**, the Greek road traffic code.
Signs are sourced from Wikimedia Commons and are in the public domain.

## Installation

```sh
npm install @road-signs/gr
```

## Sign categories

| Category | Series | Description |
| -------- | ------ | ----------- |
| `warning` | ΚΟΚ series Κ | Hazard warnings — triangular |
| `priority` | — | Right-of-way and stop signs |
| `prohibitory` | ΚΟΚ series Ρ | Prohibitions and regulatory restrictions |
| `mandatory` | — | Compulsory actions |
| `information` | ΚΟΚ series Π | Information and direction signs |

{/* USAGE:START */}

## Usage

<Tabs>
  <TabItem label="TypeScript">
    ```ts
    import { signs, getSign, getSignByCode, getSignsByCategory } from '@road-signs/gr';

    // Example below uses the real GR sign "1-curve-left" — adjust to taste.

    // All signs
    signs.length;

    // By slug ID
    const sign = getSign("1-curve-left-1-curve-left");

    // By sign code
    const s = getSignByCode("1-curve-left");

    // All warning signs
    const filtered = getSignsByCategory("warning");
    ```
  </TabItem>
  <TabItem label="React">
    ```tsx
    import { getSignsByCategory } from '@road-signs/gr';
    import { RoadSign } from '@road-signs/react';

    const warningSigns = getSignsByCategory("warning");

    export default function GRGrid() {
      return (
        <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>
          {warningSigns.map((sign) => (
            <RoadSign key={sign.id} sign={sign} size={64} />
          ))}
        </div>
      );
    }
    ```
  </TabItem>
  <TabItem label="Vue 3">
    ```vue
    <script setup>
    import { getSignsByCategory } from '@road-signs/gr';
    import { RoadSign } from '@road-signs/vue';

    const warningSigns = getSignsByCategory("warning");
    </script>

    <template>
      <div style="display:flex;gap:8px;flex-wrap:wrap">
        <RoadSign v-for="sign in warningSigns" :key="sign.id" :sign="sign" :size="64" />
      </div>
    </template>
    ```
  </TabItem>
</Tabs>

{/* USAGE:END */}

{/* COVERAGE:START */}

## Coverage

| Metric | Value |
| ------ | ----- |
| Signs scraped | 452 |
| Signs with SVG | 452 |
| Missing SVG | 0 |
| Overall | **100%** |

| Category | Scraped | With SVG | Missing | % |
| -------- | ------: | -------: | ------: | -: |
| `warning` | 59 | 59 | 0 | 100% |
| `priority` | 0 | 0 | 0 | — |
| `prohibitory` | 134 | 134 | 0 | 100% |
| `mandatory` | 0 | 0 | 0 | — |
| `information` | 259 | 259 | 0 | 100% |

**Standard:** ΚΟΚ (Κώδικας Οδικής Κυκλοφορίας) — Vienna Convention

**SVG sources:**

- [Wikimedia Commons — Road signs in Greece](https://commons.wikimedia.org/wiki/Category:SVG_road_signs_in_Greece)

:::note
Codes use romanized ΚΟΚ series: K (warning), R (prohibitory), P (information).
:::

_Coverage data is also published in the [`@road-signs/docs`](https://www.npmjs.com/package/@road-signs/docs) package._

{/* COVERAGE:END */}

## TypeScript types

```ts
import type { GRSign, GRCategory } from '@road-signs/gr';

type GRCategory =
  | 'information'
  | 'mandatory'
  | 'priority'
  | 'prohibitory'
  | 'warning';

interface GRSign {
  id: string;          // e.g. "k1-sharp-bend"
  code: string;        // sign code, e.g. "K1"
  name: string;
  description: string;
  category: GRCategory;
  svg: string;         // inline SVG string
  assets: {
    svg: string;
    png: Record<number, string>;
    jpg: Record<number, string>;
    webp: Record<number, string>;
  };
}
```
