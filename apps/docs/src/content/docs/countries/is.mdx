---
title: Iceland (IS)
description: Iceland road signs — categories, installation, and usage.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';


{/* HERO:START */}

import CountryHero from '../../../components/CountryHero.astro';

<CountryHero cc="is" />

{/* HERO:END */}

{/* QUICK_FACTS:START */}

## Quick facts

| Field | Value |
| ----- | ----- |
| Drive side | Right-hand |
| Language(s) | Icelandic |
| Regulator | Vegagerðin |
| Code format | `A01 / B01 / C01` |
| Standard | Umferðarlög — Vienna Convention |

{/* QUICK_FACTS:END */}

The `@road-signs/is` package contains Iceland road signs from the **Umferðarlög — Vienna Convention**.
Signs are sourced from Wikimedia Commons and are in the public domain.

## Installation

```sh
npm install @road-signs/is
```

## Sign categories

| Category | Series | Description |
| -------- | ------ | ----------- |
| `warning` | A series | Viðvörun — hazard warnings |
| `priority` | B series | Forgangur — right-of-way |
| `prohibitory` | C series | Bönn og takmarkanir — prohibitions |
| `mandatory` | D series | Skylda — mandatory actions |
| `information` | E–F series | Upplýsingar — information and direction |

{/* USAGE:START */}

## Usage

<Tabs>
  <TabItem label="TypeScript">
    ```ts
    import { signs, getSign, getSignByCode, getSignsByCategory } from '@road-signs/is';

    // Example below uses the real IS sign "A07.22" — adjust to taste.

    // All signs
    signs.length;

    // By slug ID
    const sign = getSign("a07-22-iceland-road-sign");

    // By sign code
    const s = getSignByCode("A07.22");

    // All warning signs
    const filtered = getSignsByCategory("warning");
    ```
  </TabItem>
  <TabItem label="React">
    ```tsx
    import { getSignsByCategory } from '@road-signs/is';
    import { RoadSign } from '@road-signs/react';

    const warningSigns = getSignsByCategory("warning");

    export default function ISGrid() {
      return (
        <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>
          {warningSigns.map((sign) => (
            <RoadSign key={sign.id} sign={sign} size={64} />
          ))}
        </div>
      );
    }
    ```
  </TabItem>
  <TabItem label="Vue 3">
    ```vue
    <script setup>
    import { getSignsByCategory } from '@road-signs/is';
    import { RoadSign } from '@road-signs/vue';

    const warningSigns = getSignsByCategory("warning");
    </script>

    <template>
      <div style="display:flex;gap:8px;flex-wrap:wrap">
        <RoadSign v-for="sign in warningSigns" :key="sign.id" :sign="sign" :size="64" />
      </div>
    </template>
    ```
  </TabItem>
</Tabs>

{/* USAGE:END */}

{/* COVERAGE:START */}

## Coverage

| Metric | Value |
| ------ | ----- |
| Signs scraped | 335 |
| Signs with SVG | 331 |
| Missing SVG | 4 |
| Overall | **99%** |

| Category | Scraped | With SVG | Missing | % |
| -------- | ------: | -------: | ------: | -: |
| `warning` | 43 | 43 | 0 | 100% |
| `priority` | 0 | 0 | 0 | — |
| `prohibitory` | 41 | 41 | 0 | 100% |
| `mandatory` | 19 | 19 | 0 | 100% |
| `information` | 232 | 228 | 4 | 98% |

**Standard:** Umferðarlög — Vienna Convention

**SVG sources:**

- [Wikimedia Commons — Road signs in Iceland](https://commons.wikimedia.org/wiki/Category:SVG_road_signs_in_Iceland)
- [Wikimedia Commons — Road signs in Iceland (general)](https://commons.wikimedia.org/wiki/Category:Road_signs_in_Iceland)

_Coverage data is also published in the [`@road-signs/docs`](https://www.npmjs.com/package/@road-signs/docs) package._

{/* COVERAGE:END */}

## TypeScript types

```ts
import type { ISSign, ISCategory } from '@road-signs/is';

type ISCategory =
  | 'information'
  | 'mandatory'
  | 'priority'
  | 'prohibitory'
  | 'warning';

interface ISSign {
  id: string;          // e.g. "a1-sharp-bend"
  code: string;        // sign code, e.g. "A1"
  name: string;
  description: string;
  category: ISCategory;
  svg: string;         // inline SVG string
  assets: {
    svg: string;
    png: Record<number, string>;
    jpg: Record<number, string>;
    webp: Record<number, string>;
  };
}
```
