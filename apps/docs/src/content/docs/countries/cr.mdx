---
title: Costa Rica (CR)
description: Costa Rica road signs — categories, installation, and usage.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';


{/* HERO:START */}

import CountryHero from '../../../components/CountryHero.astro';

<CountryHero cc="cr" />

{/* HERO:END */}

The `@road-signs/cr` package contains Costa Rica road signs from the **Manual Centroamericano de Dispositivos Uniformes para el Control del Tránsito (SIECA) — Vienna Convention variant**.
Signs are sourced from Wikimedia Commons and are in the public domain.

## Installation

```sh
npm install @road-signs/cr
```

## Sign categories

| Category | Series | Description |
| -------- | ------ | ----------- |
| `warning` | Preventivas | Señales preventivas — hazard warnings |
| `prohibitory` | Reglamentarias | Señales reglamentarias — prohibitions and regulations |
| `information` | Informativas | Señales informativas — information and direction |

## Usage

<Tabs>
  <TabItem label="TypeScript">
    ```ts
    import { signs, getSign, getSignByCode, getSignsByCategory } from '@road-signs/cr';

    // All signs
    signs.length;

    // By slug ID
    const sign = getSign("example-sign");

    // By sign code
    const s = getSignByCode("W1");

    // All warning signs
    const filtered = getSignsByCategory('warning');
    ```
  </TabItem>
  <TabItem label="React">
    ```tsx
    import { getSignsByCategory } from '@road-signs/cr';
    import { RoadSign } from '@road-signs/react';

    const warnings = getSignsByCategory('warning');

    export default function WarningGrid() {
      return (
        <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>
          {warnings.map((sign) => (
            <RoadSign key={sign.id} sign={sign} size={64} />
          ))}
        </div>
      );
    }
    ```
  </TabItem>
  <TabItem label="Vue 3">
    ```vue
    <script setup>
    import { getSignsByCategory } from '@road-signs/cr';
    import { RoadSign } from '@road-signs/vue';

    const warnings = getSignsByCategory('warning');
    </script>

    <template>
      <div style="display:flex;gap:8px;flex-wrap:wrap">
        <RoadSign v-for="sign in warnings" :key="sign.id" :sign="sign" :size="64" />
      </div>
    </template>
    ```
  </TabItem>
</Tabs>


{/* COVERAGE:START */}

## Coverage

| Metric | Value |
| ------ | ----- |
| Signs scraped | 419 |
| Signs with SVG | 419 |
| Missing SVG | 0 |
| Overall | **100%** |

| Category | Scraped | With SVG | Missing | % |
| -------- | ------: | -------: | ------: | -: |
| `warning` | 125 | 125 | 0 | 100% |
| `priority` | 0 | 0 | 0 | — |
| `prohibitory` | 194 | 194 | 0 | 100% |
| `mandatory` | 0 | 0 | 0 | — |
| `information` | 100 | 100 | 0 | 100% |

**Standard:** Manual Centroamericano de Dispositivos Uniformes para el Control del Tránsito (SIECA) — Vienna Convention variant

**SVG sources:**

- [Wikipedia — Road signs in Costa Rica](https://en.wikipedia.org/wiki/Road_signs_in_Costa_Rica)
- [Wikimedia Commons — Road signs in Costa Rica](https://commons.wikimedia.org/wiki/Category:SVG_road_signs_in_Costa_Rica)
- [Wikimedia Commons — Road signs in Costa Rica (general)](https://commons.wikimedia.org/wiki/Category:Road_signs_in_Costa_Rica)

:::note
Follows the Central American SIECA manual. Headings: Preventiva (warning), Reglamentaria (prohibitory), Informativa (information).
:::

_Coverage data is also published in the [`@road-signs/docs`](https://www.npmjs.com/package/@road-signs/docs) package._

{/* COVERAGE:END */}

## TypeScript types

```ts
import type { CRSign, CRCategory } from '@road-signs/cr';
```
