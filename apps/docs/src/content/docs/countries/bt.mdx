---
title: Bhutan (BT)
description: Bhutan road signs — categories, installation, and usage.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';




{/* HERO:START */}

import CountryHero from '../../../components/CountryHero.astro';

<CountryHero cc="bt" />

{/* HERO:END */}

{/* QUICK_FACTS:START */}

## Quick facts

| Field | Value |
| ----- | ----- |
| Drive side | Left-hand |
| Language(s) | Dzongkha |
| Regulator | RSTA |
| Standard | Bhutan Road Safety and Transport Authority (RSTA) |

{/* QUICK_FACTS:END */}

The `@road-signs/bt` package contains Bhutan road signs.

## Installation

```sh
npm install @road-signs/bt
```

{/* USAGE:START */}

## Usage

<Tabs>
  <TabItem label="TypeScript">
    ```ts
    import { signs, getSign, getSignByCode, getSignsByCategory } from '@road-signs/bt';

    // Example below uses the real BT sign "PNH1" — adjust to taste.

    // All signs
    signs.length;

    // By slug ID
    const sign = getSign("pnh1-primary-national-highway-1-shield");

    // By sign code
    const s = getSignByCode("PNH1");

    // All information signs
    const filtered = getSignsByCategory("information");
    ```
  </TabItem>
  <TabItem label="React">
    ```tsx
    import { getSignsByCategory } from '@road-signs/bt';
    import { RoadSign } from '@road-signs/react';

    const informationSigns = getSignsByCategory("information");

    export default function BTGrid() {
      return (
        <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>
          {informationSigns.map((sign) => (
            <RoadSign key={sign.id} sign={sign} size={64} />
          ))}
        </div>
      );
    }
    ```
  </TabItem>
  <TabItem label="Vue 3">
    ```vue
    <script setup>
    import { getSignsByCategory } from '@road-signs/bt';
    import { RoadSign } from '@road-signs/vue';

    const informationSigns = getSignsByCategory("information");
    </script>

    <template>
      <div style="display:flex;gap:8px;flex-wrap:wrap">
        <RoadSign v-for="sign in informationSigns" :key="sign.id" :sign="sign" :size="64" />
      </div>
    </template>
    ```
  </TabItem>
</Tabs>

{/* USAGE:END */}

{/* COVERAGE:START */}

## Coverage

| Metric | Value |
| ------ | ----- |
| Signs scraped | 6 |
| Signs with SVG | 6 |
| Missing SVG | 0 |
| Overall | **100%** |

| Category | Scraped | With SVG | Missing | % |
| -------- | ------: | -------: | ------: | -: |
| `priority` | 1 | 1 | 0 | 100% |
| `information` | 5 | 5 | 0 | 100% |

**Standard:** Bhutan Road Safety and Transport Authority (RSTA)

**SVG sources:**

- [Wikipedia — Transport in Bhutan](https://en.wikipedia.org/wiki/Transport_in_Bhutan)

:::note
Empty package: no publicly available SVG sign catalogue located.
:::

_Coverage data is also published in the [`@road-signs/docs`](https://www.npmjs.com/package/@road-signs/docs) package._

{/* COVERAGE:END */}

## TypeScript types

```ts
import type { BTSign, BTCategory } from '@road-signs/bt';
```
