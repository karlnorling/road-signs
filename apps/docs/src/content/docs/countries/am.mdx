---
title: Armenia (AM)
description: Armenia road signs — categories, installation, and usage.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';


{/* HERO:START */}

import CountryHero from '../../../components/CountryHero.astro';

<CountryHero cc="am" />

{/* HERO:END */}

The `@road-signs/am` package contains Armenia road signs from the **Vienna Convention**.
Signs are sourced from Wikimedia Commons and are in the public domain.

## Installation

```sh
npm install @road-signs/am
```

## Sign categories

| Category | Series | Description |
| -------- | ------ | ----------- |
| `warning` | Warning | Hazard and warning signs |
| `priority` | Priority | Priority and right-of-way signs |
| `prohibitory` | Prohibitory | Prohibition and restriction signs |
| `mandatory` | Mandatory | Mandatory instruction signs |
| `information` | Information / Service / Additional | Information, service, and additional plate signs |

{/* USAGE:START */}

## Usage

<Tabs>
  <TabItem label="TypeScript">
    ```ts
    import { signs, getSign, getSignByCode, getSignsByCategory } from '@road-signs/am';

    // Example below uses the real AM sign "dangerous-curve-to-the-right" — adjust to taste.

    // All signs
    signs.length;

    // By slug ID
    const sign = getSign("dangerous-curve-to-the-right-dangerous-curve-to-the-right");

    // By sign code
    const s = getSignByCode("dangerous-curve-to-the-right");

    // All warning signs
    const filtered = getSignsByCategory("warning");
    ```
  </TabItem>
  <TabItem label="React">
    ```tsx
    import { getSignsByCategory } from '@road-signs/am';
    import { RoadSign } from '@road-signs/react';

    const warningSigns = getSignsByCategory("warning");

    export default function AMGrid() {
      return (
        <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>
          {warningSigns.map((sign) => (
            <RoadSign key={sign.id} sign={sign} size={64} />
          ))}
        </div>
      );
    }
    ```
  </TabItem>
  <TabItem label="Vue 3">
    ```vue
    <script setup>
    import { getSignsByCategory } from '@road-signs/am';
    import { RoadSign } from '@road-signs/vue';

    const warningSigns = getSignsByCategory("warning");
    </script>

    <template>
      <div style="display:flex;gap:8px;flex-wrap:wrap">
        <RoadSign v-for="sign in warningSigns" :key="sign.id" :sign="sign" :size="64" />
      </div>
    </template>
    ```
  </TabItem>
</Tabs>

{/* USAGE:END */}

{/* COVERAGE:START */}

## Coverage

| Metric | Value |
| ------ | ----- |
| Signs scraped | 154 |
| Signs with SVG | 154 |
| Missing SVG | 0 |
| Overall | **100%** |

| Category | Scraped | With SVG | Missing | % |
| -------- | ------: | -------: | ------: | -: |
| `warning` | 37 | 37 | 0 | 100% |
| `priority` | 13 | 13 | 0 | 100% |
| `prohibitory` | 44 | 44 | 0 | 100% |
| `mandatory` | 14 | 14 | 0 | 100% |
| `information` | 46 | 46 | 0 | 100% |

**Standard:** Vienna Convention (ՀՀ ճանապարհային նշաններ)

**SVG sources:**

- [Wikipedia — Road signs in Armenia](https://en.wikipedia.org/wiki/Road_signs_in_Armenia)
- [Wikimedia Commons — SVG road signs in Armenia](https://commons.wikimedia.org/wiki/Category:SVG_road_signs_in_Armenia)

_Coverage data is also published in the [`@road-signs/docs`](https://www.npmjs.com/package/@road-signs/docs) package._

{/* COVERAGE:END */}

## TypeScript types

```ts
import type { AMSign, AMCategory } from '@road-signs/am';
```
