---
title: Malaysia (MY)
description: Malaysia road signs — categories, installation, and usage.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';


{/* HERO:START */}

import CountryHero from '../../../components/CountryHero.astro';

<CountryHero cc="my" />

{/* HERO:END */}

{/* QUICK_FACTS:START */}

## Quick facts

| Field | Value |
| ----- | ----- |
| Drive side | Left-hand |
| Language(s) | Malay |
| Regulator | JKR |
| Standard | Road Transport (Traffic Signs) Rules 1959 — Vienna Convention |

{/* QUICK_FACTS:END */}

The `@road-signs/my` package contains Malaysia road signs from the **Road Transport (Traffic Signs) Rules 1959 — Vienna Convention**.
Signs are sourced from Wikimedia Commons and are in the public domain.

## Installation

```sh
npm install @road-signs/my
```

## Sign categories

| Category | Series | Description |
| -------- | ------ | ----------- |
| `warning` | Warning | Hazard and warning signs |
| `prohibitory` | Regulatory | Regulatory and prohibition signs |
| `mandatory` | Instruction | Mandatory instruction signs |
| `information` | Guide / Temporary | Guide and information signs |

{/* USAGE:START */}

## Usage

<Tabs>
  <TabItem label="TypeScript">
    ```ts
    import { signs, getSign, getSignByCode, getSignsByCategory } from '@road-signs/my';

    // Example below uses the real MY sign "WD25B" — adjust to taste.

    // All signs
    signs.length;

    // By slug ID
    const sign = getSign("wd25b-malaysia-road-sign");

    // By sign code
    const s = getSignByCode("WD25B");

    // All warning signs
    const filtered = getSignsByCategory("warning");
    ```
  </TabItem>
  <TabItem label="React">
    ```tsx
    import { getSignsByCategory } from '@road-signs/my';
    import { RoadSign } from '@road-signs/react';

    const warningSigns = getSignsByCategory("warning");

    export default function MYGrid() {
      return (
        <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>
          {warningSigns.map((sign) => (
            <RoadSign key={sign.id} sign={sign} size={64} />
          ))}
        </div>
      );
    }
    ```
  </TabItem>
  <TabItem label="Vue 3">
    ```vue
    <script setup>
    import { getSignsByCategory } from '@road-signs/my';
    import { RoadSign } from '@road-signs/vue';

    const warningSigns = getSignsByCategory("warning");
    </script>

    <template>
      <div style="display:flex;gap:8px;flex-wrap:wrap">
        <RoadSign v-for="sign in warningSigns" :key="sign.id" :sign="sign" :size="64" />
      </div>
    </template>
    ```
  </TabItem>
</Tabs>

{/* USAGE:END */}

{/* COVERAGE:START */}

## Coverage

| Metric | Value |
| ------ | ----- |
| Signs scraped | 358 |
| Signs with SVG | 346 |
| Missing SVG | 12 |
| Overall | **97%** |

| Category | Scraped | With SVG | Missing | % |
| -------- | ------: | -------: | ------: | -: |
| `warning` | 76 | 75 | 1 | 99% |
| `priority` | 0 | 0 | 0 | — |
| `prohibitory` | 32 | 32 | 0 | 100% |
| `mandatory` | 17 | 17 | 0 | 100% |
| `information` | 233 | 222 | 11 | 95% |

**Standard:** Road Transport (Traffic Signs) Rules 1959 — Vienna Convention

**SVG sources:**

- [Wikipedia — Road signs in Malaysia](https://en.wikipedia.org/wiki/Road_signs_in_Malaysia)
- [Wikimedia Commons — Road signs in Malaysia](https://commons.wikimedia.org/wiki/Category:SVG_road_signs_in_Malaysia)
- [Wikimedia Commons — Road signs in Malaysia (general)](https://commons.wikimedia.org/wiki/Category:Road_signs_in_Malaysia)

_Coverage data is also published in the [`@road-signs/docs`](https://www.npmjs.com/package/@road-signs/docs) package._

{/* COVERAGE:END */}

## TypeScript types

```ts
import type { MYSign, MYCategory } from '@road-signs/my';
```
