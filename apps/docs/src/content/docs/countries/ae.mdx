---
title: United Arab Emirates (AE)
description: UAE road signs — categories, installation, and usage.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';


{/* HERO:START */}

import CountryHero from '../../../components/CountryHero.astro';

<CountryHero cc="ae" />

{/* HERO:END */}

{/* QUICK_FACTS:START */}

## Quick facts

| Field | Value |
| ----- | ----- |
| Drive side | Right-hand |
| Language(s) | Arabic, English |
| Regulator | RTA |
| Standard | British-derived, bilingual Arabic/English |

{/* QUICK_FACTS:END */}

The `@road-signs/ae` package contains UAE road signs from the **British-derived** standard.
Signs are bilingual Arabic/English and are sourced from Wikimedia Commons and are in the public domain.

## Installation

```sh
npm install @road-signs/ae
```

## Sign categories

| Category | Series | Description |
| -------- | ------ | ----------- |
| `warning` | Warning / Hazard | Hazard and warning signs |
| `prohibitory` | Regulatory | Regulatory and prohibition signs |
| `information` | Information | Information and directional signs |

{/* USAGE:START */}

## Usage

<Tabs>
  <TabItem label="TypeScript">
    ```ts
    import { signs, getSign, getSignByCode, getSignsByCategory } from '@road-signs/ae';

    // Example below uses the real AE sign "V1" — adjust to taste.

    // All signs
    signs.length;

    // By slug ID
    const sign = getSign("v1-exit");

    // By sign code
    const s = getSignByCode("V1");

    // All information signs
    const filtered = getSignsByCategory("information");
    ```
  </TabItem>
  <TabItem label="React">
    ```tsx
    import { getSignsByCategory } from '@road-signs/ae';
    import { RoadSign } from '@road-signs/react';

    const informationSigns = getSignsByCategory("information");

    export default function AEGrid() {
      return (
        <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>
          {informationSigns.map((sign) => (
            <RoadSign key={sign.id} sign={sign} size={64} />
          ))}
        </div>
      );
    }
    ```
  </TabItem>
  <TabItem label="Vue 3">
    ```vue
    <script setup>
    import { getSignsByCategory } from '@road-signs/ae';
    import { RoadSign } from '@road-signs/vue';

    const informationSigns = getSignsByCategory("information");
    </script>

    <template>
      <div style="display:flex;gap:8px;flex-wrap:wrap">
        <RoadSign v-for="sign in informationSigns" :key="sign.id" :sign="sign" :size="64" />
      </div>
    </template>
    ```
  </TabItem>
</Tabs>

{/* USAGE:END */}

{/* COVERAGE:START */}

## Coverage

| Metric | Value |
| ------ | ----- |
| Signs scraped | 136 |
| Signs with SVG | 136 |
| Missing SVG | 0 |
| Overall | **100%** |

| Category | Scraped | With SVG | Missing | % |
| -------- | ------: | -------: | ------: | -: |
| `warning` | 68 | 68 | 0 | 100% |
| `priority` | 0 | 0 | 0 | — |
| `prohibitory` | 57 | 57 | 0 | 100% |
| `mandatory` | 0 | 0 | 0 | — |
| `information` | 11 | 11 | 0 | 100% |

**Standard:** British-derived, bilingual Arabic/English

**SVG sources:**

- [Wikipedia — Road signs in the UAE](https://en.wikipedia.org/wiki/Road_signs_in_the_United_Arab_Emirates)
- [Wikimedia Commons — SVG road signs in the UAE](https://commons.wikimedia.org/wiki/Category:SVG_road_signs_in_the_United_Arab_Emirates)

_Coverage data is also published in the [`@road-signs/docs`](https://www.npmjs.com/package/@road-signs/docs) package._

{/* COVERAGE:END */}

## TypeScript types

```ts
import type { AESign, AECategory } from '@road-signs/ae';
```
