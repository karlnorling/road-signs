---
title: Tanzania (TZ)
description: Tanzania road signs — categories, installation, and usage.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';


{/* HERO:START */}

import CountryHero from '../../../components/CountryHero.astro';

<CountryHero cc="tz" />

{/* HERO:END */}

{/* QUICK_FACTS:START */}

## Quick facts

| Field | Value |
| ----- | ----- |
| Drive side | Left-hand |
| Language(s) | Swahili, English |
| Regulator | SUMATRA |
| Standard | British-derived (Road Traffic Act) |

{/* QUICK_FACTS:END */}

The `@road-signs/tz` package contains Tanzania road signs (British-derived, left-hand traffic).
Signs are sourced from Wikimedia Commons and are in the public domain.

## Installation

```sh
npm install @road-signs/tz
```

## Sign categories

| Category | Series | Description |
| -------- | ------ | ----------- |
| `warning` | Warning | Hazard and warning signs |
| `prohibitory` | Prohibitory | Prohibition and restriction signs |
| `information` | Information / Service / Additional | Information, service, and additional plate signs |

{/* USAGE:START */}

## Usage

<Tabs>
  <TabItem label="TypeScript">
    ```ts
    import { signs, getSign, getSignByCode, getSignsByCategory } from '@road-signs/tz';

    // Example below uses the real TZ sign "stop-3-way" — adjust to taste.

    // All signs
    signs.length;

    // By slug ID
    const sign = getSign("stop-3-way-stop-3-way");

    // By sign code
    const s = getSignByCode("stop-3-way");

    // All prohibitory signs
    const filtered = getSignsByCategory("prohibitory");
    ```
  </TabItem>
  <TabItem label="React">
    ```tsx
    import { getSignsByCategory } from '@road-signs/tz';
    import { RoadSign } from '@road-signs/react';

    const prohibitorySigns = getSignsByCategory("prohibitory");

    export default function TZGrid() {
      return (
        <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>
          {prohibitorySigns.map((sign) => (
            <RoadSign key={sign.id} sign={sign} size={64} />
          ))}
        </div>
      );
    }
    ```
  </TabItem>
  <TabItem label="Vue 3">
    ```vue
    <script setup>
    import { getSignsByCategory } from '@road-signs/tz';
    import { RoadSign } from '@road-signs/vue';

    const prohibitorySigns = getSignsByCategory("prohibitory");
    </script>

    <template>
      <div style="display:flex;gap:8px;flex-wrap:wrap">
        <RoadSign v-for="sign in prohibitorySigns" :key="sign.id" :sign="sign" :size="64" />
      </div>
    </template>
    ```
  </TabItem>
</Tabs>

{/* USAGE:END */}

{/* COVERAGE:START */}

## Coverage

| Metric | Value |
| ------ | ----- |
| Signs scraped | 322 |
| Signs with SVG | 322 |
| Missing SVG | 0 |
| Overall | **100%** |

| Category | Scraped | With SVG | Missing | % |
| -------- | ------: | -------: | ------: | -: |
| `warning` | 79 | 79 | 0 | 100% |
| `priority` | 0 | 0 | 0 | — |
| `prohibitory` | 223 | 223 | 0 | 100% |
| `mandatory` | 0 | 0 | 0 | — |
| `information` | 20 | 20 | 0 | 100% |

**Standard:** British-derived (Road Traffic Act)

**SVG sources:**

- [Wikipedia — Road signs in Tanzania](https://en.wikipedia.org/wiki/Road_signs_in_Tanzania)
- [Wikimedia Commons — SVG road signs in Tanzania](https://commons.wikimedia.org/wiki/Category:SVG_road_signs_in_Tanzania)

_Coverage data is also published in the [`@road-signs/docs`](https://www.npmjs.com/package/@road-signs/docs) package._

{/* COVERAGE:END */}

## TypeScript types

```ts
import type { TZSign, TZCategory } from '@road-signs/tz';
```
