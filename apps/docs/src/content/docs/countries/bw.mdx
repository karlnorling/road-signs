---
title: Botswana (BW)
description: Botswana road signs — categories, installation, and usage.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';


{/* HERO:START */}

import CountryHero from '../../../components/CountryHero.astro';

<CountryHero cc="bw" />

{/* HERO:END */}

{/* QUICK_FACTS:START */}

## Quick facts

| Field | Value |
| ----- | ----- |
| Drive side | Left-hand |
| Language(s) | English, Setswana |
| Regulator | Department of Road Transport |
| Standard | British-derived (Road Traffic Act) |

{/* QUICK_FACTS:END */}

The `@road-signs/bw` package contains Botswana road signs (British-derived (Road Traffic Act)).
Signs are sourced from Wikimedia Commons and are in the public domain.

## Installation

```sh
npm install @road-signs/bw
```

## Sign categories

| Category | Series | Description |
| -------- | ------ | ----------- |
| `warning` | Warning | Hazard and warning signs |
| `prohibitory` | Prohibitory | Prohibition and restriction signs |
| `mandatory` | Mandatory | Mandatory instruction signs |
| `information` | Information / Service / Additional | Information, service, and additional plate signs |

{/* USAGE:START */}

## Usage

<Tabs>
  <TabItem label="TypeScript">
    ```ts
    import { signs, getSign, getSignByCode, getSignsByCategory } from '@road-signs/bw';

    // Example below uses the real BW sign "A1-1" — adjust to taste.

    // All signs
    signs.length;

    // By slug ID
    const sign = getSign("a1-1-sadc-road-sign-gdls");

    // By sign code
    const s = getSignByCode("A1-1");

    // All warning signs
    const filtered = getSignsByCategory("warning");
    ```
  </TabItem>
  <TabItem label="React">
    ```tsx
    import { getSignsByCategory } from '@road-signs/bw';
    import { RoadSign } from '@road-signs/react';

    const warningSigns = getSignsByCategory("warning");

    export default function BWGrid() {
      return (
        <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>
          {warningSigns.map((sign) => (
            <RoadSign key={sign.id} sign={sign} size={64} />
          ))}
        </div>
      );
    }
    ```
  </TabItem>
  <TabItem label="Vue 3">
    ```vue
    <script setup>
    import { getSignsByCategory } from '@road-signs/bw';
    import { RoadSign } from '@road-signs/vue';

    const warningSigns = getSignsByCategory("warning");
    </script>

    <template>
      <div style="display:flex;gap:8px;flex-wrap:wrap">
        <RoadSign v-for="sign in warningSigns" :key="sign.id" :sign="sign" :size="64" />
      </div>
    </template>
    ```
  </TabItem>
</Tabs>

{/* USAGE:END */}

{/* COVERAGE:START */}

## Coverage

| Metric | Value |
| ------ | ----- |
| Signs scraped | 322 |
| Signs with SVG | 321 |
| Missing SVG | 1 |
| Overall | **100%** |

| Category | Scraped | With SVG | Missing | % |
| -------- | ------: | -------: | ------: | -: |
| `warning` | 79 | 78 | 1 | 99% |
| `priority` | 0 | 0 | 0 | — |
| `prohibitory` | 223 | 223 | 0 | 100% |
| `mandatory` | 0 | 0 | 0 | — |
| `information` | 20 | 20 | 0 | 100% |

**Standard:** British-derived (Road Traffic Act)

**SVG sources:**

- [Wikimedia Commons — SVG road signs in Botswana](https://commons.wikimedia.org/wiki/Category:SVG_road_signs_in_Botswana)

_Coverage data is also published in the [`@road-signs/docs`](https://www.npmjs.com/package/@road-signs/docs) package._

{/* COVERAGE:END */}

## TypeScript types

```ts
import type { BWSign, BWCategory } from '@road-signs/bw';
```
