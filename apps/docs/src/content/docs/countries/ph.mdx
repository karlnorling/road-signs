---
title: Philippines (PH)
description: Philippines road signs — categories, installation, and usage.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';


{/* HERO:START */}

import CountryHero from '../../../components/CountryHero.astro';

<CountryHero cc="ph" />

{/* HERO:END */}

{/* QUICK_FACTS:START */}

## Quick facts

| Field | Value |
| ----- | ----- |
| Drive side | Right-hand |
| Language(s) | Filipino, English |
| Regulator | DPWH |
| Standard | Vienna Convention (original signatory) |

{/* QUICK_FACTS:END */}

The `@road-signs/ph` package contains Philippines road signs from the **Vienna Convention**.
Signs are sourced from Wikimedia Commons and are in the public domain.

## Installation

```sh
npm install @road-signs/ph
```

## Sign categories

| Category | Series | Description |
| -------- | ------ | ----------- |
| `warning` | Warning / Hazard | Hazard and warning signs |
| `prohibitory` | Regulatory / Prohibitory | Prohibition and restriction signs |
| `mandatory` | Mandatory / Instruction | Mandatory instruction signs |
| `information` | Guide / Expressway / Combination | Information and directional signs |

{/* USAGE:START */}

## Usage

<Tabs>
  <TabItem label="TypeScript">
    ```ts
    import { signs, getSign, getSignByCode, getSignsByCategory } from '@road-signs/ph';

    // Example below uses the real PH sign "form-1-lane" — adjust to taste.

    // All signs
    signs.length;

    // By slug ID
    const sign = getSign("form-1-lane-form-1-lane");

    // By sign code
    const s = getSignByCode("form-1-lane");

    // All mandatory signs
    const filtered = getSignsByCategory("mandatory");
    ```
  </TabItem>
  <TabItem label="React">
    ```tsx
    import { getSignsByCategory } from '@road-signs/ph';
    import { RoadSign } from '@road-signs/react';

    const mandatorySigns = getSignsByCategory("mandatory");

    export default function PHGrid() {
      return (
        <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>
          {mandatorySigns.map((sign) => (
            <RoadSign key={sign.id} sign={sign} size={64} />
          ))}
        </div>
      );
    }
    ```
  </TabItem>
  <TabItem label="Vue 3">
    ```vue
    <script setup>
    import { getSignsByCategory } from '@road-signs/ph';
    import { RoadSign } from '@road-signs/vue';

    const mandatorySigns = getSignsByCategory("mandatory");
    </script>

    <template>
      <div style="display:flex;gap:8px;flex-wrap:wrap">
        <RoadSign v-for="sign in mandatorySigns" :key="sign.id" :sign="sign" :size="64" />
      </div>
    </template>
    ```
  </TabItem>
</Tabs>

{/* USAGE:END */}

{/* COVERAGE:START */}

## Coverage

| Metric | Value |
| ------ | ----- |
| Signs scraped | 435 |
| Signs with SVG | 435 |
| Missing SVG | 0 |
| Overall | **100%** |

| Category | Scraped | With SVG | Missing | % |
| -------- | ------: | -------: | ------: | -: |
| `warning` | 98 | 98 | 0 | 100% |
| `priority` | 5 | 5 | 0 | 100% |
| `prohibitory` | 66 | 66 | 0 | 100% |
| `mandatory` | 15 | 15 | 0 | 100% |
| `information` | 251 | 251 | 0 | 100% |

**Standard:** Vienna Convention (original signatory)

**SVG sources:**

- [Wikipedia — Road signs in the Philippines](https://en.wikipedia.org/wiki/Road_signs_in_the_Philippines)
- [Wikimedia Commons — SVG road signs in the Philippines](https://commons.wikimedia.org/wiki/Category:SVG_road_signs_in_the_Philippines)

_Coverage data is also published in the [`@road-signs/docs`](https://www.npmjs.com/package/@road-signs/docs) package._

{/* COVERAGE:END */}

## TypeScript types

```ts
import type { PHSign, PHCategory } from '@road-signs/ph';
```
