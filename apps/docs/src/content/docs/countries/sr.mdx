---
title: Suriname (SR)
description: Suriname road signs — categories, installation, and usage.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';


{/* HERO:START */}

import CountryHero from '../../../components/CountryHero.astro';

<CountryHero cc="sr" />

{/* HERO:END */}

The `@road-signs/sr` package contains Suriname road signs (Vienna Convention / Dutch-inherited).
Signs are sourced from Wikimedia Commons and are in the public domain.

## Installation

```sh
npm install @road-signs/sr
```

## Sign categories

| Category | Series | Description |
| -------- | ------ | ----------- |
| `warning` | A — Warning | Hazard and warning signs |
| `priority` | B — Priority | Priority and right-of-way signs |
| `prohibitory` | C — Prohibitory | Prohibition and restriction signs |
| `mandatory` | D / E — Mandatory | Mandatory instruction signs |
| `information` | F / G / H / L — Information | Information and additional signs |

{/* USAGE:START */}

## Usage

<Tabs>
  <TabItem label="TypeScript">
    ```ts
    import { signs, getSign, getSignByCode, getSignsByCategory } from '@road-signs/sr';

    // Example below uses the real SR sign "J1" — adjust to taste.

    // All signs
    signs.length;

    // By slug ID
    const sign = getSign("j1-uneven-road");

    // By sign code
    const s = getSignByCode("J1");

    // All warning signs
    const filtered = getSignsByCategory("warning");
    ```
  </TabItem>
  <TabItem label="React">
    ```tsx
    import { getSignsByCategory } from '@road-signs/sr';
    import { RoadSign } from '@road-signs/react';

    const warningSigns = getSignsByCategory("warning");

    export default function SRGrid() {
      return (
        <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>
          {warningSigns.map((sign) => (
            <RoadSign key={sign.id} sign={sign} size={64} />
          ))}
        </div>
      );
    }
    ```
  </TabItem>
  <TabItem label="Vue 3">
    ```vue
    <script setup>
    import { getSignsByCategory } from '@road-signs/sr';
    import { RoadSign } from '@road-signs/vue';

    const warningSigns = getSignsByCategory("warning");
    </script>

    <template>
      <div style="display:flex;gap:8px;flex-wrap:wrap">
        <RoadSign v-for="sign in warningSigns" :key="sign.id" :sign="sign" :size="64" />
      </div>
    </template>
    ```
  </TabItem>
</Tabs>

{/* USAGE:END */}

{/* COVERAGE:START */}

## Coverage

| Metric | Value |
| ------ | ----- |
| Signs scraped | 260 |
| Signs with SVG | 260 |
| Missing SVG | 0 |
| Overall | **100%** |

| Category | Scraped | With SVG | Missing | % |
| -------- | ------: | -------: | ------: | -: |
| `warning` | 42 | 42 | 0 | 100% |
| `priority` | 9 | 9 | 0 | 100% |
| `prohibitory` | 8 | 8 | 0 | 100% |
| `mandatory` | 94 | 94 | 0 | 100% |
| `information` | 107 | 107 | 0 | 100% |

**Standard:** Vienna Convention / Dutch-influenced (Verkeerstekenbesluit)

**SVG sources:**

- [Wikimedia Commons — Road signs in Suriname](https://commons.wikimedia.org/wiki/Category:Road_signs_in_Suriname)
- [Wikimedia Commons — SVG road signs in the Netherlands](https://commons.wikimedia.org/wiki/Category:SVG_road_signs_in_the_Netherlands)
- [Wikipedia — Road signs in the Netherlands](https://en.wikipedia.org/wiki/Road_signs_in_the_Netherlands)

:::note
No Suriname-specific SVG sign diagrams exist on Wikimedia Commons. Suriname uses a Dutch-derived sign system inherited from the colonial period, virtually identical to Netherlands road signs (same Vienna Convention base, same series lettering A–L). This package mirrors the Netherlands sign set, following the same pattern as Monaco→France and San Marino→Italy.
:::

_Coverage data is also published in the [`@road-signs/docs`](https://www.npmjs.com/package/@road-signs/docs) package._

{/* COVERAGE:END */}

## TypeScript types

```ts
import type { SRSign, SRCategory } from '@road-signs/sr';
```
