---
title: Czech Republic (CZ)
description: Czech road signs — categories, installation, and usage.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';


{/* HERO:START */}

import CountryHero from '../../../components/CountryHero.astro';

<CountryHero cc="cz" />

{/* HERO:END */}

The `@road-signs/cz` package contains Czech Republic road signs under **zákon č. 361/2000 Sb.**
Signs are sourced from Wikimedia Commons and are in the public domain.

## Installation

```sh
npm install @road-signs/cz
```

## Sign categories

| Category | Series | Description |
| -------- | ------ | ----------- |
| `warning` | A series | Výstražné značky — hazard warnings |
| `priority` | B series | Značky upravující přednost — right-of-way |
| `prohibitory` | B/C series | Zákazové a příkazové značky — prohibitions and restrictions |
| `mandatory` | C series | Příkazové značky — compulsory actions |
| `information` | IP/IJ/IS series | Informativní značky — information and service |

## Usage

<Tabs>
  <TabItem label="TypeScript">
    ```ts
    import { signs, getSign, getSignByCode, getSignsByCategory } from '@road-signs/cz';

    // All signs
    signs.length;

    // By slug ID
    const sign = getSign("a1-sharp-bend");

    // By sign code
    const s = getSignByCode("A1");

    // All warning signs
    const filtered = getSignsByCategory('warning');
    ```
  </TabItem>
  <TabItem label="React">
    ```tsx
    import { getSignsByCategory } from '@road-signs/cz';
    import { RoadSign } from '@road-signs/react';

    const warnings = getSignsByCategory('warning');

    export default function WarningGrid() {
      return (
        <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>
          {warnings.map((sign) => (
            <RoadSign key={sign.id} sign={sign} size={64} />
          ))}
        </div>
      );
    }
    ```
  </TabItem>
  <TabItem label="Vue 3">
    ```vue
    <script setup>
    import { getSignsByCategory } from '@road-signs/cz';
    import { RoadSign } from '@road-signs/vue';

    const warnings = getSignsByCategory('warning');
    </script>

    <template>
      <div style="display:flex;gap:8px;flex-wrap:wrap">
        <RoadSign v-for="sign in warnings" :key="sign.id" :sign="sign" :size="64" />
      </div>
    </template>
    ```
  </TabItem>
</Tabs>


{/* COVERAGE:START */}

## Coverage

| Metric | Value |
| ------ | ----- |
| Signs scraped | 275 |
| Signs with SVG | 274 |
| Missing SVG | 1 |
| Overall | **100%** |

| Category | Scraped | With SVG | Missing | % |
| -------- | ------: | -------: | ------: | -: |
| `warning` | 44 | 44 | 0 | 100% |
| `priority` | 8 | 8 | 0 | 100% |
| `prohibitory` | 43 | 43 | 0 | 100% |
| `mandatory` | 34 | 34 | 0 | 100% |
| `information` | 146 | 145 | 1 | 99% |

**Standard:** Zákon č. 361/2000 Sb. — Vienna Convention

**SVG sources:**

- [Wikimedia Commons — Road signs in the Czech Republic](https://commons.wikimedia.org/wiki/Category:SVG_road_signs_in_the_Czech_Republic)

_Coverage data is also published in the [`@road-signs/docs`](https://www.npmjs.com/package/@road-signs/docs) package._

{/* COVERAGE:END */}

## TypeScript types

```ts
import type { CZSign, CZCategory } from '@road-signs/cz';

type CZCategory =
  | 'information'
  | 'mandatory'
  | 'priority'
  | 'prohibitory'
  | 'warning';

interface CZSign {
  id: string;          // e.g. "a1-sharp-bend"
  code: string;        // sign code, e.g. "A1"
  name: string;
  description: string;
  category: CZCategory;
  svg: string;         // inline SVG string
  assets: {
    svg: string;
    png: Record<number, string>;
    jpg: Record<number, string>;
    webp: Record<number, string>;
  };
}
```
